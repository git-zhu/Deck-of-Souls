# 阶段五：护符 — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-05-21-phase5-relics-design.md`

## Tasks

1. `RelicData.gd` + `tools/build_relics.py` + `data/relics/*.tres`
2. `RunState.relics` + reset
3. `RelicService.gd`
4. `DataRegistry` load relics
5. `CombatController` + `RelicService.apply_combat_start`
6. `MerchantService` `grant_relic`；更新 `scrap_paper.tres`
7. `Main` header 护符数
8. `tools/relic_service_test.gd` + docs + **git commit**
